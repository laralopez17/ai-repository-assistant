services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      SQLITE_DB_PATH: /app/data/ai_repository_assistant.db
    volumes:
      - ./data:/app/data
      - ${REPO_MOUNT_SOURCE:-.}:/workspace:ro
